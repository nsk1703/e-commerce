{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Category edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class=" btn btn-success my-2" style="float: right" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <a class="btn btn-primary my-2" href="{{ path('adminCategories_index') }}">Back</a>

    {{ form_start(delete_form) }}
        <input class="btn btn-danger" style="float: right" type="submit" value="Delete">
    {{ form_end(delete_form) }}
{% endblock %}

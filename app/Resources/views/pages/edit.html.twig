{% extends 'baseAdmin.html.twig' %}

{% block body %}
{#    <h1>Page edit</h1>#}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <a href="{{ path('adminPages_index') }}" class="btn btn btn-primary my-2">Back</a>
        <input class="btn btn-success my-2" style="float: right" type="submit" value="Edit"/>
    {{ form_end(edit_form) }}
    {{ form_start(delete_form) }}
    <input class="btn btn-danger my-2" style="float: right" type="submit" value="Delete">
    {{ form_end(delete_form) }}

{% endblock %}

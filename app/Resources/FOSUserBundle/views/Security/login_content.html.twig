{% trans_default_domain 'FOSUserBundle' %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}

<div class="container my-3">
    <div class="row">
        <div class="col-md-4">
            <h4>Not yet register? </h4>
            <p>
                We invite you to subscribe
                to pass your command to eat very well
            </p>
            <a href="{{ path('fos_user_registration_register') }}" class="btn btn-primary">Subscribe</a>
        </div>
        <div class="col-md-8">
            <h4 class="text-center">Login</h4>
            <form class="form-signin" action="{{ path("fos_user_security_check") }}" method="post">
                {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                {% endif %}
                <div class="form-group">
                    <input class="form-control" placeholder="{{ 'security.login.username'|trans }}" type="text" id="username" name="_username" value="{{ last_username }}" required="required" autocomplete="username" />
                    <label for="username"></label>
                </div>
                <div class="form-group">
                    <input class="form-control" placeholder="{{ 'security.login.password'|trans }}" type="password" id="password" name="_password" required="required" autocomplete="current-password" />
                    <label for="password"></label>
                </div>
                <input class="btn btn-lg btn-primary btn-block" type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
            </form>
        </div>
    </div>
</div>




{#                <div class="span10">#}
{#                    <a href="{{ path('fos_user_resetting_request') }}">Forget Password ?</a>#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
{#    </div>#}

